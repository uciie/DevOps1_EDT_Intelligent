package com.example.backend.record;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import java.util.List;

//class AIPlanningResponseTest {

   // @Test
    //void testRecordCreation() {
        // Préparation des données
       // AIProposedTask task = new AIProposedTask("T1", "Test", 30, List.of(), "Reasoning");
       // AIPlanningResponse response = new AIPlanningResponse("Global info", List.of(task));

        // Vérifications (Assertions)
       // assertEquals("Global info", response.globalExplanation());
        //assertEquals(1, response.tasks().size());
        //assertEquals("T1", response.tasks().get(0).id());
    //}
//}